# Copiază în .env (nu urca .env pe GitHub):
# cp .env.example .env
# Apoi generează SECRET_KEY: python -c "import secrets; print(secrets.token_hex(32))"

SECRET_KEY=inlocuieste-cu-cheie-generata
ALGORITHM=HS256
EXPIRARE_TOKEN_MINUTE=30
DATABASE_PATH=sarcini.db
